import * as actionTypes from '../actionTypes.jsx';


let initialState = {
    category: null,
    author: null,
};


export default function filters (state=initialState, action) {
    // TODO: write your own code here!

    // In the meantime, do something with unused variables so that ESLint
    // doesn't complain. You can safely remove these lines.
    actionTypes;
    action;

    return state;
}
